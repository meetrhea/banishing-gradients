---
export const prerender = true;

import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { authors } from '../data/authors';
---

<BaseLayout title="Our Contributors" description="Meet the distinguished journalists of Banishing Gradients">
  <Header />

  <main class="flex-1">
    <div class="container mx-auto px-4 py-8">
      <!-- Masthead-style header -->
      <div class="text-center border-b-4 accent-border pb-6 mb-8">
        <h1 class="text-4xl md:text-5xl font-black mb-2" style="font-family: 'Playfair Display', Georgia, serif;">
          THE STAFF
        </h1>
        <p class="text-gray-600 italic text-lg">
          Meet the Distinguished Contributors of Banishing Gradients
        </p>
        <div class="mt-4 text-sm text-gray-500 uppercase tracking-wider">
          Est. 2024 &bull; Serving the Public Interest Since Someone Had To
        </div>
      </div>

      <!-- Editorial note box -->
      <div class="bg-gray-50 border-l-4 accent-border p-6 mb-12 max-w-3xl mx-auto">
        <p class="text-sm leading-relaxed text-gray-700">
          <span class="font-bold">FROM THE EDITOR'S DESK:</span> The journalists listed below represent the finest minds in AI coverage—or at least the ones who answered our classified ad. Each has sworn a sacred oath to speak truth to power, question authority, and never, under any circumstances, use the word "synergy" unironically. Reader correspondence may be directed to our offices, where it will be read by someone, eventually.
        </p>
      </div>

      <!-- Contributors grid - newspaper staff box style -->
      <div class="max-w-5xl mx-auto">
        {authors.map((author, index) => (
          <div class={`py-8 ${index < authors.length - 1 ? 'border-b border-gray-300' : ''}`}>
            <div class="grid grid-cols-1 md:grid-cols-12 gap-6">
              <!-- Name and title column -->
              <div class="md:col-span-4">
                <h2 class="text-2xl font-bold mb-1" style="font-family: 'Playfair Display', Georgia, serif;">
                  {author.name}
                </h2>
                <p class="accent-text font-medium uppercase text-sm tracking-wide mb-2">
                  {author.title}
                </p>
                <p class="text-xs text-gray-500">
                  {author.yearsAtPaper} {author.yearsAtPaper === 1 ? 'year' : 'years'} with the paper
                </p>
              </div>

              <!-- Bio column -->
              <div class="md:col-span-8">
                <p class="text-gray-700 leading-relaxed text-sm md:text-base">
                  {author.bio}
                </p>
              </div>
            </div>
          </div>
        ))}
      </div>

      <!-- Footer note -->
      <div class="text-center mt-12 pt-8 border-t-2 border-gray-200">
        <p class="text-xs text-gray-500 uppercase tracking-widest mb-2">
          — Inquiries & Tips —
        </p>
        <p class="text-sm text-gray-600 italic">
          For story tips, corrections, or complaints about our coverage,<br />
          please write a strongly-worded letter and leave it on a park bench.
        </p>
      </div>
    </div>
  </main>

  <Footer />
</BaseLayout>
